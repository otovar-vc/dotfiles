# Common ignored files that may not always be present in a project.

# Worktrees and workspaces
.claude/worktrees/
.worktrees/
.workspaces/

# Local tooling
mise.local.toml

# Local agent configurations
.claude/settings.local.json

# Code Knowledge Graph (CKG) artifacts
.codegraph/

# Local tmp/ folder that keeps writes/reads within a workspace.
tmp/
